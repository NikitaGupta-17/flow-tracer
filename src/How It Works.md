How It Works

Point FlowTracer at your repositories and ask a question.

It:

Builds a manifest of function signatures + imports
Uses an LLM to identify relevant files
Performs a deeper pass to generate the flow diagram + walkthrough
Maintains context for follow-up questions